;; (use-package clojure-mode
;;   :pin melpa-stable
;;   :commands clojure-mode
;;   :config
;;   (add-hook 'clojure-mode-hook
;;             (lambda ()
;;               (eldoc-mode))))

;; (use-package cider
;;   :pin melpa-stable
;;   :commands cider-jack-in)
